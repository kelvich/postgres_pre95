#include "c.h"
#include "parse.h"
#include "atoms.h"
#include "pg_lisp.h"

HandleRetrieveRuleDef(parsetree,trigger_relation,
		     trigger_qual,is_instead)
     LispValue parsetree,trigger_relation;
     LispValue trigger_qual,is_instead;
{
    
}

HandleReplaceRuleDef(parsetree,trigger_relation,
		     trigger_qual,is_instead)
     LispValue parsetree,trigger_relation;
     LispValue trigger_qual,is_instead;
{
    
}
